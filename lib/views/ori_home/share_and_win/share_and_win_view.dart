import 'package:flutter/material.dart';

class ShareAndWinView extends StatelessWidget {
  const ShareAndWinView({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Share & Win'),);
  }
}
