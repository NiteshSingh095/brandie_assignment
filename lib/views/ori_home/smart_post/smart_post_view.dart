import 'package:flutter/material.dart';

class SmartPostView extends StatelessWidget {
  const SmartPostView({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Smart Post'),);
  }
}